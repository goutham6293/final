// Example JavaScript for interactive components
// In a larger project you might fetch news via an API.
// Here we simply log a message when the page loads.
window.addEventListener('DOMContentLoaded', () => {
    console.log('My News Site loaded');
});
